[project]
name = "comfyui-layout-sort"
version = "0.17.0"
description = "Auto-arranges the current ComfyUI workflow into a clean layered layout based on its data flow — whole graph, a selection, or inside a group frame — via node execution, a button, the right-click menu or Alt+Shift+S. Fully deterministic, no network calls."
requires-python = ">=3.9"
dependencies = []

[project.urls]
Repository = "https://github.com/ssain3d-lgtm/Comfyui-Layout-Sort"

[tool.comfy]
DisplayName = "ComfyUI Layout Sort"
PublisherId = "ssain3d"
